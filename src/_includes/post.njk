---
layout: layout.njk
---

<section class="hero post-hero">
  <p class="eyebrow">{{ category }}</p>
  <h1>{{ title }}</h1>
  <p class="hero-text">{{ date | readableDate }}</p>
</section>

<section class="featured post-body">
  {{ content | safe }}
</section>
